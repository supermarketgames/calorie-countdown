<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.supermarketgames.caloriecountdown"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CalorieCountdown">
        <activity
            android:name=".LandingPageActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.CalorieCountdown.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BaseGameActivity"
            android:label="@string/title_activity_game"
            android:theme="@style/Theme.CalorieCountdown.NoActionBar" />
        <activity
            android:name=".HighScoresActivity"
            android:label="@string/title_activity_high_scores"
            android:theme="@style/Theme.CalorieCountdown.NoActionBar">
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings"
            android:theme="@style/Theme.CalorieCountdown.NoActionBar">
        </activity>
    </application>

</manifest>
